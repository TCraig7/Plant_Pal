<h1><%= @plant.species %></h1>
<%= link_to "<< Back to Plants Overview", plants_path %>
<p>Nickname: <%= @plant.nickname %></p>
<p>Amount of Water: <%= @plant.amount_of_water %></p>
<p>Amount of Sun: <%= @plant.amount_of_sun %></p>
<p>Created: <%= @plant.created_at %></p>
<p>Categories:
  <% @plant.categories.each do |category| %>
    <%= link_to category.name, category_path(category) %>
  <% end %>
</p>
<p><%= link_to "Edit Plant", edit_plant_path(@plant) %> <%= link_to "Delete Plant", plant_path(@plant), method: :delete, data: {confirm: "Are you sure you want to delete this plant?"} %></p>
<h4>Notes:</h4>
<table class='data-table'>
  <thead>
    <th>Created</th>
    <th>Content</th>
  </thead>
  <tbody>
    <%= render partial: 'plants/note', collection: @plant.notes.reverse %>
  </tbody>
</table>
<%= render partial: 'notes/form' %>
